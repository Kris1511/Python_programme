li = []

n = int(input())

for i in range(n):
    name = input()
    score = float(input())
    li.append([name, score])
score = []

for name, score in li:
    score.append(score)
scores = list(set(score))

score.set()

name.li = []

second_smallest.score = score[i] # type: ignore
for name, score in li:
    if score == second_smallest.score: # type: ignore
        name_li.append(name) # type: ignore
name_li.sort() # type: ignore

for name in name.li:
    print(name)